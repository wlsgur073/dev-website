# 네이밍 규칙

- 문서 폴더명은 코드 패키지명과 동일하게 유지한다.
- 도메인 폴더는 `/src/main/java/com/devwebsite/backend/<domain>`의 1-depth 패키지명을 사용한다.
- 레이어 폴더는 패키지명과 동일하게 유지한다(예: controller, service).
- 클래스 문서 파일명은 클래스명과 동일한 `PascalCase`를 사용한다.
- 문서 기본 진입점은 `README.md`로 통일한다.
